dependencies:
  cache_directories:
    - ~/.android
    - ~/android

general:
    artifacts:
        - /home/ubuntu/comicsdbclient/app/build/outputs/apk/

dependencies:
    pre:
        - echo y | android update sdk --no-ui --all --filter build-tools-23.0.1,android-23